                                        List of Errors checked by the Tester

1. Empty Parameters

-- check if "alphabet" field is empty
empty_alphabet.json

-- check if "states" field is empty
empty_states.json

-- check if "finals" field is empty
empty_finals.json

-- check if "transitions" field is empty
empty_transitions.json

2. Missing Parameters

-- check if "name" field is Missing
missing_name.json

-- check if "alphabet" field is Missing
missing_alphabet.json

-- check if "blank" field is Missing
missing_blank.json

-- check if "states" field is Missing
missing_states.json

-- check if "initial" field is Missing
missing_initial.json

-- check if "finals" field is Missing
missing_finals.json

-- check if "transitions" field is Missing
missing_transitions.json

-- check if "read" field is Missing
missing_read.json

-- check if "to_state" field is Missing
missing_to_state.json

-- check if "write" field is Missing
missing_to_state.json

-- check if "action" field is Missing
missing_to_action.json


2. Invalid Parameters

-- check if the initial state exists in states list

-- check if final states exist in states list

-- check if states in transitions exist in states list

-- check if to_state in each transition exists in states list

-- check if symbols in transitions exist in alphabet


3. Incoherance

-- check that HALT is not a transition state

-- check that each action is either LEFT or RIGHT

-- check that each character of the alphabet is a string of length 1


4. Input Errors

-- check that input only contains characters from the alphabet

-- check that blank symbol is not in the input

-- check that the correct number of arguments are given

-- check that the file is a json

-- check that the json is in a correct format
